
# LaunchFlow docs

- Overview: orchestrates CI/CD pipelines across GitDigital.
- Endpoints:
  - `GET /health`
  - `POST /api/v1/pipeline/trigger`
- Local dev: `npm install && npm run dev`
- Docker: `docker-compose up --build`